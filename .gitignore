# Roam
__pycache__/
*.pyc
.venv/
venv/
# dedicated Chrome profile (logins live here — never commit)
profile/
*.profile/
# local config (no secrets, but machine-specific)
config.json
# local selector-memory DB (private browsing memory — never commit)
memory.db
*.db
# stray lock files
*.conf.lock
# scratch / test artifacts
_*.png
*.png
test-results/
